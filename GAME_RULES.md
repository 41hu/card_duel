# 卡牌对决 — 完整游戏规则与技术规格

## 一、游戏概述

双人对战回合制卡牌游戏。双方各选一名角色，在 11 格横线棋盘上通过卡牌进行攻防博弈。先清空对方 HP 且对方无法复活者获胜。

---

## 二、回合系统

### 2.1 回合阶段
每回合分为 4 个阶段：
1. **判定阶段** — 结算 DoT（灼烧/中毒），检查死亡/冻结
2. **摸牌阶段** — 自动抽 2 张牌
3. **出牌阶段** — 玩家自由出牌（行动点限制）
4. **弃牌阶段** — 超出手牌上限需弃至达标

### 2.2 行动点
- 攻击行动点：2 点/回合
- 位移行动点：1 点/回合
- 功能行动点：1 点/回合（术士 +1）
- 三种独立计算，每回合刷新

### 2.3 手牌上限
- = 到己方板边距离 + 1
- 左边玩家（P1）：position + 1
- 右边玩家（P2）：(10 - position) + 1
- 起手 4 张，每回合抽 2 张

---

## 三、战斗系统

### 3.1 伤害类型

| 类型 | 距离 | 消耗 | 响应 |
|---|---|---|---|
| **近战** (near) | 必须贴脸（距离=0）| 1 攻击点 | 格挡（减半）|
| **远程** (range) | 伤害=面板-距离 | 1 攻击点 | 牵制（等额扣除）|
| **魔法** (magic) | 无视距离 | 1 攻击点 | 闪避（完全抵消）|
| **重击** (heavy) | 贴脸，近战+3 | 2 攻击点 | 同近战 |
| **穿心** (pierce) | 远程结算后+3 | 2 攻击点 | 同远程 |
| **吟唱** (chant) | 无视距离，魔法+3 | 2 攻击点 | 同魔法 |

### 3.2 距离计算
`distance = |pos_a - pos_b| - 1`
- 贴脸：distance = 0（相邻格）

### 3.3 响应系统
- **格挡**：使用近战卡响应 → 伤害减半（向下取整）
- **牵制**：使用远程卡响应 → 仅对远程/魔法生效，扣除(面板-距离)
- **闪避**：使用魔法卡响应 → 完全抵消，对任何攻击类型生效

### 3.4 防具
3 种防具（近战/远程/法术），各 3 次耐久：
- 第 1 次：完全免疫
- 第 2、3 次：减半（向下取整）
- 1 个防具槽，可主动换装

### 3.5 武器
3 类武器各 4 把，全局不重复：
```
近战：烈焰剑(近战+2)、霜咬(命中后对方下回合位移=0)、嗜血(近战≥3伤害回2HP)、突刺(近战+1，移动牌位移到贴脸额外+3)
远程：长弓(远程+1，距离衰减-1)、连弩(牵制额外-2)、鹰眼(命中后查看对方手牌)、毒牙(中毒-1×2层可叠加)
法术：贤者之书(魔法+2)、灼烧(-2×2回合，再次命中刷新)、时滞(命中后对方下回合攻击-1)、共鸣(本回合已出过其他攻击则本次+2，失败攻击不算)
```

---

## 四、卡牌池（78 张）

| 类别 | 卡牌 | 数量 |
|---|---|---|
| 基础攻击 | 近战、远程、魔法 | 各 8 |
| 强化攻击 | 重击、穿心、吟唱 | 各 3 |
| 位移 | 移动 | 7 |
| 功能 | 吸引 2、威慑 2、冻结 2、摧毁 3、夺取 2 | 11 |
| 回复 | +3 HP ×3、+5 HP ×2 | 5 |
| 强化 | 近战+1 ×2、远程+1 ×2、魔法+1 ×2 | 6 |
| 免费 | 天赐 4、陷阱 3 | 7 |
| 武器牌 | 近战武器 2、远程武器 2、法术武器 2 | 6 |
| 防具 | 近战 1、远程 1、法术 1 | 3 |
| **总计** | | **78** |

---

## 五、角色（9 名）

| 角色 | HP | 近 | 远 | 魔 | 技能 |
|---|---|---|---|---|---|
| 剑士 | 28 | 7 | 3 | 2 | 近战命中后：抽1牌或回2HP（每回合限一）|
| 弓手 | 24 | 2 | 8 | 2 | 每回合首次普通远程不消耗攻击点 |
| 法师 | 22 | 2 | 2 | 8 | 弃1手牌，本次魔法+2（每回合限一）|
| 圣骑士 | 36 | 4 | 1 | 3 | 每回合首次受伤-2（计算后，最低0）|
| 刺客 | 22 | 7 | 5 | 1 | 每回合免费移动1格（独立于位移点）|
| 牧师 | 28 | 2 | 4 | 6 | 使用回复卡额外+2；受到DoT伤害后清除该类DoT |
| 狂战士 | 28 | 8 | 2 | 2 | 受直接攻击后获得狂化（近战+1，持续3回合，可叠加）|
| 术士 | 24 | 2 | 3 | 5 | 功能点+1；未用功能牌则回合结束抽1张 |
| 快枪手 | 26 | 2 | 6 | 2 | 远程双发：固定消耗2攻击点，造成两段伤害，两段各需一次响应 |

> 快枪手双发：远程/穿心固定消耗2攻击点，造成**两段**伤害（每段独立计算、各需一次响应）。普通远程每段 = `(远攻−距离)/2` 向下取整（最小0）；穿心每段 = `(远攻+3−距离)/2` 向下取整（最小0）。两段视为两次攻击：圣骑士首伤-2只减免第一段，狂战士狂化触发两次，武器命中特效每段触发。

---

## 六、特殊机制

### 6.1 复活
HP ≤ 0 时 → 弃光手牌 → 抽 4 张 → 自动使用回复卡 → HP > 0 复活，否则永久淘汰。
- 抽到天赐可额外抽 2 张（仅一次）
- 复活清冻结，保留 DoT
- 无限次数

### 6.2 冻结
跳过下个出牌阶段。同一玩家不可连续被冻结。

### 6.3 陷阱
放在任意空格，首次到达触发 3 伤害后销毁。同格仅放 1 个（不可堆叠），自踩生效。

### 6.4 摧毁
可选：盲丢对方 1 张手牌，或指定摧毁对方一件装备。

### 6.5 夺取
盲抽对方 1 张手牌。

### 6.6 移动推人
贴脸时向对方方向移动可推着对方一起移动。

### 6.7 天赐
抽 2 张牌。免费，每回合限 1 张。

### 6.8 数值强化
近战/远程/魔法面板永久 +1。弃牌堆重洗后可循环叠加。

### 6.9 装备流程
武器牌 → 幻化（随机一把未生成的武器）→ 弹出确认框 → 装备 / 丢弃。
防具直接装上。

---

## 七、BP 禁选流程
1. P1 禁用 1 个角色
2. P2 禁用 1 个角色
3. P1 选择角色
4. P2 选择角色
5. 随机先手，进入战斗

---

## 八、技术架构

### 8.1 技术栈
- Godot 4.7.1+
- GDScript 2.0
- WebSocket（联网对战）

### 8.2 项目结构（以 WORKFLOW.md 为准）
```
card_duel/
├── project.godot              # autoload: Config, Network, LocalGame, BackHandler, McpInteractionServer
├── scenes/                    # main_menu / bp_scene / battle_scene / settlement / server
├── scripts/
│   ├── autoload/              # config(数据) network(WebSocket) local_game(本地/人机) back_handler(返回键) mcp_interaction_server(调试桥)
│   ├── core/                  # match_state(服务端权威状态机) + 7个系统组件 + ai_player(人机AI)
│   ├── data/                  # card_data / character_data / equip_data（纯数据表）
│   ├── server/                # server_main（TCPServer+房间管理）
│   └── ui/                    # main_menu / bp_ui / battle_ui / settlement_ui / components/
```

### 8.3 Autoload 顺序
1. Config — 静态数据
2. Network — WebSocket 客户端
3. LocalGame — 本地自我对战/人机对战（模拟 Network 接口）
4. BackHandler — 返回键双击退出
5. McpInteractionServer — 调试桥（仅开发用）

### 8.4 联网架构
```
ECS 云服务器 (Godot headless, 权威服务端, 17890)
    ↕ WebSocket (JSON)
电脑客户端 ── 手机客户端 (Android 导出)
```
- 服务端跑完整 `match_state.gd`；客户端纯 UI，操作转发至服务端
- 本地/人机模式：`LocalGame` 直接驱动同一套 MatchState，行为与联机一致
- 完整协议见 WORKFLOW.md / API_REFERENCE.md

---

## 九、Godot 4.x 编程注意事项

1. **match 不能写单行**：每个分支必须换行缩进
2. **lambda 不能含多语句**：`func(): a; b` 不合法，要拆成方法
3. **`get_node("X")` 只能找直接子节点**：深层节点用成员变量引用
4. **`BoxContainer.ALIGNMENT_CENTER`** 可能报 enum 警告，用 `alignment = 1` 代替
5. **枚举值**：`CardCategory.NEAR = 0`, `RANGE = 1`, `MAGIC = 2`, `HEAVY_STRIKE = 3`, `PIERCE = 4`, `CHANT = 5`, `MOVE = 6`, `ATTRACT = 7`, `DETER = 8`, `FREEZE = 9`, `DESTROY = 10`, `SEIZE = 11`, `HEAL_3 = 12`, `HEAL_5 = 13`, `NEAR_BUFF = 14`, `RANGE_BUFF = 15`, `MAGIC_BUFF = 16`, `BLESSING = 17`, `TRAP = 18`, `NEAR_WEAPON = 19`, `RANGE_WEAPON = 20`, `MAGIC_WEAPON = 21`, `NEAR_ARMOR = 22`, `RANGE_ARMOR = 23`, `MAGIC_ARMOR = 24`
6. **枚举值**：`Phase.MAIN_MENU=0, BP_PHASE=1, PLAYER_TURN=2, RESPONSE_WINDOW=3, RESURRECTING=4, GAME_OVER=5`
7. **枚举值**：`APType.NONE=0, ATTACK=1, MOVE=2, FUNCTION=3`
8. **枚举值**：`TurnPhase.JUDGMENT=0, DRAW=1, ACTION=2, DISCARD=3`
9. **布局**：`PRESET_FULL_RECT=15` 填满父容器；`PRESET_CENTER=8` 居中，配合 `alignment=1` 居中子元素
10. **场景文件**：`.tscn` 是文本格式，UID 可以不填让 Godot 自动生成
11. **WebSocket 服务端**：用 `TCPServer + WebSocketPeer.accept_stream()`，不用 `--headless`（Windows 限制）
12. **持久化数据**卡牌手牌格式 `{uid: int, type_id: String}`，type_id 对应 config.gd 中 `CARD_DB` 的 key

---

## 十、关键 Bug 修复记录

1. **GAME_OVER 被覆盖**：`_resolve_combat` 末尾不能无条件设 `phase = PLAYER_TURN`，要检查 GAME_OVER
2. **`_start_turn` 检查 GAME_OVER**：在函数开头加 `if phase == GAME_OVER: return`
3. **`_advance_to_next_player` 检查 GAME_OVER**：同上
4. **共鸣武器取错对象**：`_resolve_combat` 中检查的是 `defender["weapon"]` 应改为 `attacker["weapon"]`，或者删除该段（在 `_handle_magic_attack` 中已正确处理）
5. **BP 节点查找失败**：程序化创建的节点不能用 `get_node("Name")` 找孙子节点，要用成员变量存储引用
6. **复活流程**：`_handle_death` → 抽 4 → 自动用回复 → 检查 HP > 0 → 成功/失败
